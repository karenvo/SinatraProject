<h1> Coffee Page </h1>
<form action = "/products" method = "POST">
    <% @coffees.each do |coffee| %>
        <br><input type = "checkbox" name = "product_ids[]" id="<%= coffee.id %>" value="<%= coffee.id %>">
        <%= coffee.name %></input>
        <br><%= coffee.brand %>
        <br>$<%= coffee.price %>
    <% end %>
    <br>
    <input type="button" class = "button" value = "Add to Cart">
</form>

<br><a href= "/products">Back to Product Page </a>